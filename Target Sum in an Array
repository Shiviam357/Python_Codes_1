def find_target(arr, target):
    t="Target not found"
    T=[0,0]
    #print(arr[0:1],arr[1:2])
    for  i in range(1,len(arr)):
        if (arr[i-1]+arr[i]==target): T[0]=i-1; T[1]=i
    if T==[0,0]: return t
    else: return T
